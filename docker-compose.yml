version: '3.8'

services:
  # 백엔드 서비스
  backend:
    # ECR에 올라간 이미지를 사용할 것이므로 build 대신 image 사용
    image: 481961344502.dkr.ecr.ap-northeast-2.amazonaws.com/hotelhub-backend:latest
    restart: always
    environment:
      # GitHub Secrets에 저장된 DB 정보를 컨테이너에 전달
      - SPRING_DATASOURCE_URL=${DB_URL}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - SERVER_PORT=${SERVER_PORT}

    management-backend: # 2. 관리자/사업자 백엔드
    image: YOUR_ECR_URI/management-backend:latest # 실제 ECR 이미지 이름으로 변경
    restart: always
    environment:
      - SPRING_DATASOURCE_URL=${DB_URL}
      - SPRING_DATASOURCE_USERNAME=${DB_USERNAME}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - SERVER_PORT=${SERVER_PORT}

  # 프론트엔드 서비스
  frontend:
    image: 481961344502.dkr.ecr.ap-northeast-2.amazonaws.com/hotelhub-frontend:latest
    restart: always
    
  admin-frontend: # 2. 관리자 프론트엔드
    image: 481961344502.dkr.ecr.ap-northeast-2.amazonaws.com/admin-frontend:latest # 실제 ECR 이미지 이름으로 변경
    restart: always

  business-frontend: # 3. 사업자 프론트엔드
    image: 481961344502.dkr.ecr.ap-northeast-2.amazonaws.com/bus-frontend:latest # 실제 ECR 이미지 이름으로 변경
    restart: always

  # Nginx 리버스 프록시
  nginx:
    image: nginx:1.21-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./proxy/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - backend
      - management-backend
      - frontend
      - admin-frontend
      - business-frontend

  # Certbot (SSL 인증서 발급/갱신)
  certbot:
    image: certbot/certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
